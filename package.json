{
  "name": "@leokun/relax",
  "version": "1.0.0",
  "main": "dist/app.js",
  "type": "module",
  "scripts": {
    "explame": "docker compose  -f ./docker/docker-compose.yml up -d && cd ./explame && npm run dev",
    "lint": "eslint --fix ."
  },
  "devDependencies": {
    "eslint": "^8.39.0",
    "typescript": "^4",
    "@leokun/eslint-config": "^0.0.4",
    "@leokun/typescript-config": "^0.0.3"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  }
}